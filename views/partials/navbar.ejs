<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand fw-bold" href="/journals">✈ TravelJournal</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-center">
                <% if (currentUser) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/journals">My Journals</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/journals/new">New Entry</a>
                    </li>
                    <li class="nav-item">
                        <span class="nav-link text-warning fw-semibold">
                            <i class="bi bi-compass"></i> <%= currentUser.travelStyle %>
                        </span>
                    </li>
                    <li class="nav-item">
                        <span class="nav-link text-light opacity-75">Hi, <%= currentUser.username %></span>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-outline-light btn-sm ms-2" href="/logout">Logout</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-primary btn-sm ms-2" href="/register">Register</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
